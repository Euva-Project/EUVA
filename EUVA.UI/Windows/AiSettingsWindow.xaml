<Window x:Class="EUVA.UI.Windows.AiSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Universal AI Settings" Height="420" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E2E" Foreground="#CDD6F4"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4" />
            <Setter Property="Margin" Value="0,5,0,2" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244" />
            <Setter Property="Foreground" Value="#CDD6F4" />
            <Setter Property="BorderBrush" Value="#45475A" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#313244" />
            <Setter Property="Foreground" Value="#CDD6F4" />
            <Setter Property="BorderBrush" Value="#45475A" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#313244" />
            <Setter Property="Foreground" Value="#11111B" />
            <Setter Property="BorderBrush" Value="#45475A" />
            <Setter Property="Padding" Value="4" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#89B4FA" />
            <Setter Property="Foreground" Value="#11111B" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="API Key (Encrypted with DPAPI):" FontWeight="SemiBold" />
        <PasswordBox Grid.Row="1" x:Name="TxtApiKey" Margin="0,0,0,10" />

        <TextBlock Grid.Row="2" Text="Base URL / API Endpoint (OpenRouter/Ollama/etc):" FontWeight="SemiBold" ToolTip="The full URL to the completions endpoint. Example: https://api.groq.com/openai/v1/chat/completions" />
        <TextBox Grid.Row="3" x:Name="TxtBaseUrl" Margin="0,0,0,10" 
                 ToolTip="Example: https://openrouter.ai/api/v1/chat/completions" />

        <TextBlock Grid.Row="4" Text="Model Name (Required):" FontWeight="SemiBold" />
        <TextBox Grid.Row="5" x:Name="TxtModelName" Margin="0,0,0,10" 
                 ToolTip="Example: llama3-70b-8192 or deepseek-coder" />

        <TextBlock Grid.Row="6" Text="System Prompt Override (Advanced):" FontWeight="SemiBold" />
        <TextBox Grid.Row="7" Grid.RowSpan="2" x:Name="TxtSystemPrompt" TextWrapping="Wrap" AcceptsReturn="True" 
                 FontFamily="Consolas" FontSize="11" Margin="0,0,0,15"
                 Text="Analyze this decompiled C code..." />

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Margin="0,0,10,0" Width="80" Click="BtnCancel_Click" Background="#45475A" Foreground="#CDD6F4" />
            <Button Content="Save Settings" Width="100" Click="BtnSave_Click" />
        </StackPanel>
    </Grid>
</Window>
